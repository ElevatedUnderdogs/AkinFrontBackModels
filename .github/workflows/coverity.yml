# name: akin
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#   workflow_dispatch: 
  
# jobs:
#   build:
#     runs-on: macOS-latest
#     steps:
#       - name: Checkout Source
#         uses: actions/checkout@v3

#       - name: Coverity Scan
#         uses: synopsys-sig/synopsys-action@v1.11.0
#         with:
#           ### SCANNING: Required fields
#           coverity_url: ${{ secrets.COVERITY_URL }}
#           coverity_user: ${{ secrets.COVERITY_USER }}
#           coverity_passphrase: ${{ secrets.COVERITY_PASSPHRASE }}
          
#           ### Coverity Connect users - Uncomment below
#           coverity_local: true
        
#           ### POLICY ENFORCEMENT: Uncomment to break build on policy
#           coverity_policy_view: 'Outstanding Issues' 

#           ### Block PR if defects are found
#           coverity_prComment_enabled: true
#           coverity_prComment_fail_on_defects: true
#           github_token: ${{ secrets.GITHUB_TOKEN }} # Required when PR comments are enabled
